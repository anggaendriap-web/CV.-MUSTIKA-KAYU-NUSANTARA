rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default open access for operational intranet factory collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
